[project]
name = "bruce"
version = "0.1.0"
description = "Nano Agent Project: Bruce"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.16.0",
    "strictyaml>=1.0",
    "click>=8.0",
]

[project.scripts]
bruce = "main:main"

[tool.pyright]
venvPath = "."
venv = ".venv"